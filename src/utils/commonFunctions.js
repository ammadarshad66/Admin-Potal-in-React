//  ================= Set value in localStorage ===============
export const setItemToStore = (key, payload, store) =>
  store.setItem(key, payload);
//  ================= navigation redirect page ===============
